# Generated by Gestures
# Taken over by ME
# (this syntax is really simple)

# Swipe threshold (0-100)
swipe_threshold 0

# Gestures
gesture swipe right 4 eww --detach open main_window
gesture swipe  left 4 eww kill
gesture swipe right 3 bspc desktop -f prev.local
gesture swipe  left 3 bspc desktop -f next.local
gesture swipe  down 4 powermenu
gesture swipe    up 4 rofi -show drun -show-icons
gesture swipe    up 3 rofi -show window -show-icons
